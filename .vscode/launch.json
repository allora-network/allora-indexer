{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Pump",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}",
            "args": ["--node",
                "https://allora-rpc.testnet.allora.network:443",
                "--cliApp",
                "allorad",
                "--conn", 
                "postgres://postgres:password@localhost:5432/allora"
            ]
        }
    ]
}